export const SET_LIST = (state, list) => {
  state.list = list
}

export const SET_STATUS_LIST = (state, list) => {
  state.statusList = list
}

export const SET_LOADING = (state, flag) => {
  state.loading = flag
}

export const SET_DOCTOR = (state, doctorId) => {
  state.doctor = doctorId
}
